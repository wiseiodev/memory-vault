{
  "name": "extension",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "build": "wxt build",
    "check": "biome check --write --unsafe .",
    "check:ci": "biome check .",
    "dev": "wxt",
    "postinstall": "wxt prepare",
    "test": "node -e \"process.exit(0)\"",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@memory-vault/extension-contract": "workspace:*"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.24",
    "typescript": "^6.0.2",
    "wxt": "^0.20.20"
  }
}
